* Attribute

	-the attribute basically use to pass the data one servlet to another.
	-the servlet programmer can pass the information(data) from one servlet to another servlet using by attribute.
	-we can reuse the same object again and again.
	-in attribute their are three methods-
    		1) public void setAttribute(String name);
    		2) public void getattribute(String name);
    		3) public void removeattribute();